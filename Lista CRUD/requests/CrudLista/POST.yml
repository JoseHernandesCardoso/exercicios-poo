info:
  name: POST
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8090/produtos
  body:
    type: json
    data: |-
      {
        "id" : 2,
        "nome" : "fodas2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​