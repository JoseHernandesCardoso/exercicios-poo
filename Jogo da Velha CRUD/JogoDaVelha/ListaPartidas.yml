info:
  name: ListaPartidas
  type: http

http:
  method: GET
  url: http://localhost:8080/partidas
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
